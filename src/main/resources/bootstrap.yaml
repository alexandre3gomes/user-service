spring:
  cloud:
    config:
      name: user-service
      discovery:
        service-id: config-service
        enabled: true
eureka:
  client:
    region: default
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://localhost:8761/eureka/